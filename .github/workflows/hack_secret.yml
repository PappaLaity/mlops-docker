name: Show GitHub Secret

on:
  workflow_dispatch:

jobs:
  extract-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Print secret (unsafe demo)
        run: |
          echo "secret: ${{secrets.API_KEY}}"
          echo "variable: ${{vars.VARIABLE}}"